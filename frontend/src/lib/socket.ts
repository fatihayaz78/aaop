// Socket.IO client — placeholder for real-time updates
// Will be initialized in a future sprint when WebSocket integration is built

export const SOCKET_URL = process.env.NEXT_PUBLIC_WS_URL || "http://localhost:8000";
